Based on the provided source, here are the short answers to your questions regarding the **TA-Mem** framework:

- **What becomes a memory format?** Conversation history is transformed into **structured notes** that include summaries, keywords, involved persons, facts, events, temporal references, and semantic tags.
- **How is memory represented?** It is represented as a **memory page (\(M_i\))** containing the original dialogue segment, the structured memory note (\(N_i\)), and a conversation timestamp (\(t\)).
- **Where is it stored?** Memories are stored in a **multi-indexed memory database** that supports both key-based lookups and vector-space similarity searches.
- **What kinds of memory exist?** The system primarily utilizes **episodic memory notes** that are topic-segmented and information-dense.
- **How memories are retrieved?** Retrieval is performed by an autonomous **tool-augmented agent** that selects specific tools (such as string queries for names/tags or similarity-based fetches for events) based on the user's question.
- **How are retrieved memories prioritized?** Memories are prioritized using **cosine similarity ranking** for vector searches, and the retrieval agent cognitively decides if further context is needed after reasoning on initial results.
- **How does existing memory update after new context?** When new context is received, the memory extraction agent **segments the input into new chunks** and extracts episodic information for storage.
- **How are memories compressed/combined?** The system **chunks input context** based on semantic correlation and topic shifts, extracting dense summaries and facts rather than storing raw content alone.
- **How memory forget unnecessary context?** A **per-QA-session memory cache** temporarily stores retrieved pages and drops redundant content to reduce token usage. The tools also provide a more "filtered context" compared to traditional methods.
- **What happens when memories disagree or conflict occur?** The retrieval agent uses **timestamps** associated with memory pages to resolve conflicts and select the most recent information.
- **How is time handled?** Time is handled through **conversation timestamps** and **temporal references** deducted from the context and associated with specific events.
- **How is memory importance determined?** Importance is determined during extraction by identifying **semantically important keywords** and facts. During retrieval, the agent determines relevance based on the specific query.
- **Does memory depend on current task?** **Yes**, the retrieval agent autonomously explores the memory and selects tools specifically based on the **user input question**.
- **Who makes memory decisions?** **LLM agents** make the decisions—specifically a memory extraction agent for storage and a retrieval agent for fetching information.
- **When does memory management happen?** Memory extraction is an **offline process**, while retrieval and reasoning happen dynamically **during the QA session**.
- **Does future usefulness influence memory?** The provided source **does not explicitly state** that future usefulness is a metric for storage decisions in the TA-Mem framework.
- **How expensive is management?** The system is designed to be **token-efficient** (averaging 3755 tokens per question), though the agentic loop introduces **computational latency**.